import React from 'react';import { Banknote, CheckCircle2, CreditCard, Landmark, ReceiptText, Wallet } from 'lucide-react';import { AppLayout } from '../components/AppLayout.jsx';import { cashService } from '../services/api.js';
const money=v=>new Intl.NumberFormat('es-CL',{style:'currency',currency:'CLP',maximumFractionDigits:0}).format(Number(v||0));
export function ArqueoPage(props){const today=new Date().toISOString().slice(0,10),[data,setData]=React.useState(null),[cash,setCash]=React.useState(''),[expenses,setExpenses]=React.useState('0'),[msg,setMsg]=React.useState('');React.useEffect(()=>{cashService.get(today).then(setData).catch(()=>{})},[today]);const sales=Number(data?.['Subtotal Ventas']||0),expected=Number(data?.['Total Efectivo']||0)-Number(expenses||0),diff=Number(cash||0)-expected;async function close(){try{await cashService.create({fecha:today,subtotal_ventas:sales,total_efectivo:Number(cash||0),gastos_dia:Number(expenses||0)});setMsg('Caja cerrada correctamente.')}catch{setMsg('No se pudo cerrar la caja. Revisa los datos.')}}return <AppLayout {...props} active="arqueo" title="Cierre de caja" eyebrow="Resumen del turno"><div className="closing-banner"><div><span>Turno actual</span><h2>Resumen de hoy</h2><p>{new Intl.DateTimeFormat('es-CL',{dateStyle:'full'}).format(new Date())}</p></div><div><small>Estado de caja</small><strong><i/> Caja abierta</strong></div></div><section className="metric-grid three"><Metric icon={<ReceiptText/>} label="Ventas realizadas" value={data?'Registradas':'0'} sub="durante el turno"/><Metric icon={<Wallet/>} label="Total vendido" value={money(sales)} sub="venta bruta"/><Metric icon={<Banknote/>} label="Efectivo esperado" value={money(expected)} sub="menos gastos"/></section><div className="cash-grid"><section className="data-panel"><div className="data-panel__top"><div><h2>Ventas por medio de pago</h2><p>Recopilación del turno actual.</p></div></div><div className="payment-summary"><div><span className="pay-icon cash"><Banknote/></span><p><strong>Efectivo</strong><small>Ventas en caja</small></p><b>{money(data?.['Total Efectivo'])}</b></div><div><span className="pay-icon debit"><Landmark/></span><p><strong>Débito</strong><small>Pagos procesados</small></p><b>—</b></div><div><span className="pay-icon credit"><CreditCard/></span><p><strong>Crédito</strong><small>Pagos procesados</small></p><b>—</b></div></div></section><aside className="checkout close-box"><h2>Realizar arqueo</h2><label>Monto contado en caja<input type="number" value={cash} onChange={e=>setCash(e.target.value)} placeholder="$ 0"/></label><label>Gastos del turno<input type="number" value={expenses} onChange={e=>setExpenses(e.target.value)}/></label><div className={`difference ${diff===0?'even':diff>0?'up':'down'}`}><span>Diferencia</span><strong>{money(diff)}</strong></div><button className="primary-button full" onClick={close}><CheckCircle2/> Cerrar caja</button>{msg&&<p className="feedback center">{msg}</p>}</aside></div></AppLayout>}
function Metric({icon,label,value,sub}){return <article className="metric-card"><span className="metric-card__icon yellow">{icon}</span><div><small>{label}</small><strong>{value}</strong><em>{sub}</em></div></article>}
