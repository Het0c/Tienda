services:

  dev:
    build: .
    volumes:
      - .:/workspace
    depends_on:
      - mariadb

  mariadb:
    image: mariadb:11.8
    environment:
      MARIADB_ROOT_PASSWORD: root
      MARIADB_DATABASE: tienda_online
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
